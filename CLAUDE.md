# NodeMind

Local-first mind map canvas by Windshifter. See `README.md` for setup and deployment.

Versioning: edit only `version.json` via `node scripts/version.mjs` (see `docs/VERSIONING.md`). Do not hardcode version/build elsewhere.
